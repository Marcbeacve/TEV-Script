# CUOFC -> TEV Semantic Calculus Correspondence V0

Status: **research-only, post-V1 experimental, non-normative**.

The partial research translation is `CUOFC ⇀ TEV Semantic Calculus`. It may return `EXACT`, `DERIVED`, `PARTIAL`, `OUT_OF_SCOPE`, or `REJECT`. It does not define TEVScript language meaning, runtime behavior, ABI, build, certification, truth, or promotion authority.

Positive correspondences: finite `Field -> SemanticFieldV0`; finite `Transformation -> RuleField + Apply`; `Center -> CenterContextV0/context_hash`; finite `Evidence -> EvidenceField`; `Observation -> rule + orthogonal knowledge/external effects`; declared environment laws -> `DomainLawV0`; project/prepare/replay/commit -> TEV application regimes.

Required negative correspondences: actual external reality is `OUT_OF_SCOPE`; truth by self-declaration is `REJECT`; an unbounded continuum is `PARTIAL` and needs a computable abstraction plus preservation obligation; unstratified self-certification is `REJECT`; an omniscient center is `PARTIAL` because TEV centers are bounded by view, knowledge, authority, frame and policy.

Authority flows only from independently governed TEV specifications into TEVScript. CUOFC may motivate or analyze TEV, but no production or normative authority surface may import this research module. Because this candidate is add-only and production imports remain unchanged, correspondence presence cannot become an executable dependency.

`LANGUAGE_STABLE=NO`. No merge, release, truth, or promotion authority is granted.
