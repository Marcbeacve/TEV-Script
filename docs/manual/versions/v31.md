# V3.1 — CURRENT 3.1.0

La versión de lenguaje **CURRENT** es `3.1.0`, perfil `total_core`. El paquete candidato que la distribuye es `3.1.2`; son dominios distintos.

Novedad estructural: Program IR V5 Total-Core compone proceso semántico, Field/Transformation/Apply, unidades V4 `pure|recursive|effects`, `invoke_v4`, admisiones de prueba externas y ejecución por quanta/checkpoints.

La release metadata actual mantiene `MERGE_AUTHORITY = False` y `PUBLICATION_AUTHORITY = False`; completar documentación o tests no cambia esa autoridad.