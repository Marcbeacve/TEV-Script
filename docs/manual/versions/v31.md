# V3.1 — CURRENT Total-Core

## Estado

```text
language_version = 3.1.0
source_profile    = total_core
status            = CURRENT
package current   = 3.1.2
Program IR        = V5 total_core
runtime ABI       = v5-total-v1
checkpoint        = v5-total-checkpoint-v1
```

Ésta es la ruta recomendada para nuevos procesos raíz.

## Header

```text
process Demo version "3.1.0";
```

## Qué añade sobre semantic-process

Total-Core conserva facts/Field/Transformations/control V3 y añade composición de children V4:

```text
unit Calc profile pure;
label A = invoke_v4 Calc result app.calc B;
```

Profiles child actuales:

```text
pure | recursive | effects
```

## Program IR root

```text
TEV_SCRIPT_PROGRAM_IR_V5_TOTAL_CORE_V1
```

contiene Field/Transformations, units, proof admissions, instruction table, limits, authority y `program_hash`.

## Computación abierta

La ejecución global se representa con quanta finitos. Status del quantum:

```text
HALTED
SUSPENDED
```

Continuations/checkpoints ligan cada epoch.

## Proof admissions

Son input externo Program IR/API. Source 3.1 no puede fabricarse una verificación.

## Effects

Children effects conservan evidencia/scenario y receipts V4. Physical commit continúa fuera del runtime portable.

## Compatibilidad

3.1 es aditivo:

- V2 sigue siendo 2.0.0;
- V4 sigue siendo child authority;
- V3 semantic-process sigue 3.0.0;
- APIs históricas siguen clasificadas como compatibility donde la matriz las mantiene.

## Package 3.1.2

El package current no cambia la version del lenguaje. El predecesor publicado inmediato del package es 3.1.1; `packaging/v31` conserva además el package 3.1.0 como predecessor archivado. Son dominios separados.

## Release authority

Los metadatos actuales solicitan stable admission pero mantienen:

```text
PUBLICATION_AUTHORITY = False
MERGE_AUTHORITY       = False
```

Un PASS focal/documental no concede publicación/merge.

## Autoridad

- `spec/TEV_SCRIPT_3_1_PLATFORM.md`
- `spec/TEV_SCRIPT_V31_TOTAL_CORE.md`
- `spec/TEV_SCRIPT_VERSION_MATRIX.json`
- `tev_script/version.py`
- `tev_script/release_metadata_v31.py`
