# Internals de TEVScript

Esta sección explica arquitectura, no añade sintaxis pública. Orden: pipeline, identidad semántica, estratos IR, fronteras runtime, pruebas/capacidades y arquitectura de validación. La autoridad normativa sigue en `spec/`; estos documentos conectan esas piezas para implementadores.